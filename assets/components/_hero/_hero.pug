mixin hero(data) 
  section.hero
    .container
      header.hero__top
        +logo()
        +socials()
      address.hero__address
        .hero__email admin@youremail.com.au
        +phone-number()
